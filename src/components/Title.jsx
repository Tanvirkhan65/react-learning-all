import React from 'react';

const Title = () => {
    console.log('tile');
    return (
        <div>
            <h1>increment functionality</h1>
        </div>
    );
};

export default React.memo(Title);
